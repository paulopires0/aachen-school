# Graph Neural Networks for Crystals — DGM AI School 26, Aachen
#
#   python -m venv .venv && source .venv/bin/activate     (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
#
# Torch comes as a CPU build here: the workshop trains in about a minute on a laptop
# and a CUDA download is ~2 GB larger. See the README if you do want the GPU build.
--extra-index-url https://download.pytorch.org/whl/cpu

numpy>=1.26
torch>=2.2
matplotlib>=3.8
pymatgen>=2024.1.1
scikit-learn>=1.3

# to open the notebooks
jupyterlab>=4.0
